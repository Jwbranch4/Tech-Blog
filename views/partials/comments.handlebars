<div class="list-group">
  {{#each this}}
    <section>
      <div></div>
      <br />
      <div>
        {{comment_content}}
        <br />
        -
        {{user.username}},
        {{format_date created_at}}
      </div>
    </section>
  {{/each}}
</div>